name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  PYTHON_VERSION: "3.11"

jobs:
  lint:
    name: Lint & Static Analysis
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install ruff mypy

      - name: Run Ruff linter
        run: ruff check src/ --ignore E501,F821

  test:
    name: Run Test Suite
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-asyncio

      - name: Run Unit & Architecture Tests
        run: pytest tests/test_architecture_integration.py tests/test_dependency_injection.py -v
        env:
          SOLANA_CLUSTER: devnet
          QUICKNODE_ENDPOINT: https://api.devnet.solana.com
          JUPITER_API_BASE_URL: https://quote-api.jup.ag/v6
          WALLET_PRIVATE_KEY: 3RbhFZ9Up8PzV5ujdRv15yQtXGQnyU4PNmpfReQaqxKFfJbi59JVBjdVFZNsnEgUv8aSvURwWvQyvKGMX91CAC71
          WALLET_ADDRESS: 9WzDXwBbmkg8ZTbNMqUxvQRAyrZzDsGYdLVL9zYtAWWM
