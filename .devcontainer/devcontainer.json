{
  "name": "Candlestick Bot",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "remoteUser": "developer",
  "postCreateCommand": "pip install --user -r requirements.txt",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  }
}